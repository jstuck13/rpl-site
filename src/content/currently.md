---
note: ""
---

<!--
  `note` above is an optional, single-line editorial aside for the top of
  /currently — something like "Bucky Irving FC has quietly won three
  straight." Written only when there's actually something worth saying, not
  on a schedule. Leave it empty ("") to skip the note entirely; the computed
  blocks on the page (standings movement, award-race gaps, streaks) don't
  need this file touched to stay current — they're derived straight from
  results.json and refresh on their own every time `npm run data` runs.

  Keep it to one line — the parser here only reads a single frontmatter
  value, not a markdown body. Basic inline formatting (**bold**, links) is
  fine, since it's rendered through the same markdown parser as the rest of
  the site's copy.
-->
